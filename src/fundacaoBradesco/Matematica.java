package fundacaoBradesco;

public class Matematica {
	int maior(int first, int second) {
		if(first > second) {
			return first;
		}else {
			return second;
		}
	}
	double soma(double first, double second) {
		double s = first + second;
		return s;
	}
}
